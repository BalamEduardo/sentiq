import { AppPlaceholderPage } from "@/components/layout/app-placeholder-page";

export default function RestaurantSettingsPage() {
  return (
    <AppPlaceholderPage
      title="Configuracion"
      description="La configuracion del restaurante, usuarios y enlaces se implementara en tickets posteriores."
    />
  );
}
