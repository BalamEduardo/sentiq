import { AppPlaceholderPage } from "@/components/layout/app-placeholder-page";

export default function RestaurantResponsesPage() {
  return (
    <AppPlaceholderPage
      title="Respuestas"
      description="El listado y detalle de respuestas de feedback se implementaran en tickets posteriores."
    />
  );
}
